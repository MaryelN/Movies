{{ form_start(form) }}

  {{ form_row(form.q) }}

  <h4>Genre</h4>
  {{ form_row(form.genre) }}

  <h4 class="mt-3">Reviews</h4>
  <div class="row mt-3">
    <div class="col-md-6">
      {{ form_row(form.max) }}
    </div>
    <div class="col-md-6">
    {{ form_row(form.min) }}
    </div>
  </div>

  <button type="submit" class="btn btn-primary -100 mt-5">Filtrer</button>
{{ form_end(form) }}